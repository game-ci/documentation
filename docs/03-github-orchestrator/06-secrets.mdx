# Secrets

Orchestrator securely transfers secrets to remote build containers using each provider's native
secrets system.

## Kubernetes

Secrets are created as native **Kubernetes Secret** objects and mounted as environment variables in
job containers.

## AWS

Secrets are stored in **AWS Secrets Manager** and injected into Fargate tasks as environment
variables.

## 🔐 Managing Secrets

For pulling secrets from external sources (e.g. GCP Secret Manager, AWS Secrets Manager) into
Orchestrator parameters, see [Configuration Override](advanced-topics/configuration-override).
