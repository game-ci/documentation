# Retained Workspace Mode

Caches the entire project folder. This option provides the best responsiveness, but also can consume
lots of storage.

You can specify a maximum number of retained workspaces, only one job can use a retained workspace
at one time. Each retained workspace consumes more cloud storage.
