# Custom Hooks

Custom commands can be injected into the standard build workflow via yaml or files.

```yaml
- name: example hook
  hook: |
    step:
      - before
    commands: |
      echo "hello world!"
```
