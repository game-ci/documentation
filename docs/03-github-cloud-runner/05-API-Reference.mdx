# API Reference

## Cloud Runner Core API

Cloud Runner uses the core parameters of Game-CI.

## Modes

```md
- hash (hash all folder contents)
- cache-push (push to cache)
- cache-pull (pull from cache)
- remote-cli-pre-build (sets up a repository, usually before a game-ci build)
- print-input (prints all input)
- cli-build (runs a cloud runner build)
- garbage-collect (runs garbage collection)
- list-resources (lists active resources)
- list-worfklow (lists running workflows)
- watch (follows logs of a running workflow)
- remote-cli-post-build (runs a cloud runner build)
```

## Main Parameters

```md
- region
- GITHUB_REPOSITORY
- GITHUB_REF || branch || GitSHA
- cloudRunnerBuilderPlatform
- cloudRunnerBranch
- cloudRunnerCluster
- cloudRunnerCluster
- cloudRunnerCpu
- cloudRunnerMemory
- customJob
- customStepFiles
- customHookFiles
- customJobHooks
- postBuildSteps
- preBuildSteps
- readInputFromOverrideList
- readInputOverrideCommand
- awsBaseStackName
- kubeConfig
- kubeVolume
- kubeVolumeSize
- kubeStorageClass
- cacheKey
- cloudRunnerTests ||cloudRunnerDebug
- cloudRunnerDebugTree
- cloudRunnerDebugEnv
- watchToEnd
- useSharedLargePackages
- useSharedBuilder
- useLz4Compression
- retainWorkspaces
- maxRetainedWorkspaces
- constantGarbageCollection
- garbageCollectionMaxAge
```

## Command Line Only Parameters

```md
- populateOverride
- cachePushFrom
- cachePushTo
- artifactName
- select
```

## Other Environment Variables

```md
USE_IL2CPP - Set to `false`
```
