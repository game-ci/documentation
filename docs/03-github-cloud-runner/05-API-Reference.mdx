# API Reference

## Cloud Runner Core API

Cloud Runner uses the core parameters of Game-CI.

## Modes

```md
Run / CLI Run List workflows List resources Watch Garbage Collect
```

## Main Parameters

```md
region GITHUB_REPOSITORY GITHUB_REF || branch || GitSHA cloudRunnerBuilderPlatform cloudRunnerBranch
cloudRunnerCluster cloudRunnerCluster cloudRunnerCpu cloudRunnerMemory customJob customStepFiles
customHookFiles customJobHooks postBuildSteps preBuildSteps readInputFromOverrideList
readInputOverrideCommand awsBaseStackName kubeConfig kubeVolume kubeVolumeSize kubeStorageClass
cacheKey cloudRunnerTests ||cloudRunnerDebug cloudRunnerDebugTree cloudRunnerDebugEnv watchToEnd
useSharedLargePackages useSharedBuilder useLz4Compression retainWorkspaces maxRetainedWorkspaces
constantGarbageCollection garbageCollectionMaxAge
```

## Command Line Only Parameters

```md
...
```

## Other Environment Variables

```md
...
```
